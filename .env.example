# Shopify Admin API — required for the phone-number cross-check.
STORE_NAME=your-store.myshopify.com
ADMIN_API_KEY=shpat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional — only needed if you switch the engine to Gemini (the default
# pipeline is 100% local and needs no key).
# GEMINI_API_KEY=
